\input{header}

\begin{document}

\frontpage{%
    Struktury danych% nazwa kursu
}{%
    Informatyczne Systemy Automatyki% kierunek
}{%
    sroda TP $15^{15}-16^{55}$% termin
}{-}{-}{%
    Pawel Bijak 2XXXXX% imie, nazwisko, nr albumu
}{%
    https://github.com/bijakpawel/SD_Kolejki% link do projektu
}{%
    data oddania projektu% data
}{%
    2% nr miniprojektu
}

\pagestyle{empty}

\section{Cel projektu}

Celem projektu jest zaimplementowanie własnymi środkami kolejki priorytetowej
typu MAX w języku C++ w oparciu o dwie różne struktury danych oraz przeprowadzenie
pomiarów czasu wykonania podstawowych operacji w celu praktycznej weryfikacji
ich teoretycznej złożoności obliczeniowej. Wybrane struktury to:
\begin{itemize}
    \item kopiec binarny przechowywany w tablicy,
    \item jednokierunkowa lista wiązana posortowana malejąco po priorytecie.
\end{itemize}

\section{Charakterystyka kolejki priorytetowej}

Kolejka priorytetowa to abstrakcyjny typ danych, w którym każdy element posiada
dodatkową wartość zwaną priorytetem. Kolejność obsługi elementów wyznacza
priorytet, a nie moment dodania do struktury. W wariancie MAX pierwszeństwo ma
element o najwyższym priorytecie.

W projekcie każdy element jest reprezentowany przez strukturę zawierającą:
\begin{itemize}
    \item wartość elementu (\texttt{int}),
    \item priorytet (\texttt{int}),
    \item numer wstawienia (\texttt{long long}) -- pole pomocnicze dla strategii FIFO.
\end{itemize}

\subsection{Strategia FIFO przy równych priorytetach}

W przypadku wielu elementów o tym samym priorytecie przyjęto strategię FIFO
(first in, first out). Każdy element przy wstawianiu otrzymuje rosnący numer
wstawienia. Przy porównaniu dwóch elementów o tym samym priorytecie
pierwszeństwo ma ten o mniejszym numerze wstawienia (czyli wstawiony wcześniej).

W przypadku operacji \texttt{modifyKey} przyjęto, że modyfikowany element
zachowuje swój pierwotny numer wstawienia. Oznacza to, że element po zmianie
priorytetu nie traktuje się jako ,,nowy''. Jest to spójne z intuicją, że
zmieniamy jedynie priorytet istniejącego elementu, a nie wstawiamy go na nowo.

\section{Implementacje}

\subsection{Kopiec binarny}

Kopiec binarny to drzewo binarne spełniające własność kopca: priorytet rodzica
jest większy lub równy priorytetowi każdego z jego dzieci (kopiec MAX).
W~projekcie kopiec został zaimplementowany jako tablica dynamiczna z indeksowaniem
0--bazowym, gdzie:
\begin{itemize}
    \item rodzic węzła $i$ znajduje się pod indeksem $\lfloor (i-1)/2 \rfloor$,
    \item lewe dziecko węzła $i$ znajduje się pod indeksem $2i+1$,
    \item prawe dziecko węzła $i$ znajduje się pod indeksem $2i+2$.
\end{itemize}

W razie braku miejsca tablica jest powiększana dwukrotnie. Operacje przywracające
własność kopca to \texttt{kopcujWGore} (po wstawieniu elementu) oraz
\texttt{kopcujWDol} (po wyciągnięciu maksimum lub modyfikacji w dół).

\subsubsection*{Złożoność operacji}
\begin{itemize}
    \item \texttt{insert} -- $O(\log n)$ (przesuwanie nowego elementu do właściwej pozycji),
    \item \texttt{extractMax} -- $O(\log n)$ (przywrócenie własności kopca po usunięciu korzenia),
    \item \texttt{peek} -- $O(1)$ (korzeń tablicy),
    \item \texttt{modifyKey} -- $O(n)$ (liniowe wyszukiwanie elementu po wartości) plus $O(\log n)$ na naprawę kopca,
    \item \texttt{rozmiar} -- $O(1)$.
\end{itemize}

\subsection{Lista wiązana}

Lista wiązana jednokierunkowa posortowana malejąco po priorytecie. Element
o~najwyższym priorytecie znajduje się zawsze na początku listy (głowa). Przy
wstawianiu element trafia za wszystkie istniejące elementy o tym samym lub
wyższym priorytecie, co realizuje strategię FIFO.

\subsubsection*{Złożoność operacji}
\begin{itemize}
    \item \texttt{insert} -- $O(n)$ (znalezienie odpowiedniej pozycji),
    \item \texttt{extractMax} -- $O(1)$ (zdjęcie głowy listy),
    \item \texttt{peek} -- $O(1)$ (zwrócenie głowy bez modyfikacji),
    \item \texttt{modifyKey} -- $O(n)$ (wyszukanie i ponowne wstawienie),
    \item \texttt{rozmiar} -- $O(1)$ (utrzymywany licznik).
\end{itemize}

\section{Metodyka pomiarów}

\subsection{Środowisko}

Pomiary wykonano na komputerze osobistym pod systemem Windows 10. Kompilacji
dokonano kompilatorem g++ (MSYS2/UCRT64, wersja 15.2.0) z flagami
\texttt{-O2 -std=c++17}. Czas mierzono z wykorzystaniem \texttt{std::chrono::steady\_clock}
z dokładnością do nanosekund.

\subsection{Sposób generowania danych}

Wartości elementów losowano z zakresu $[-1000, 1000]$. Priorytety losowano
z~zakresu $[0, 5n - 1]$, gdzie $n$ jest aktualnym rozmiarem kolejki. Zakres
priorytetów został wybrany jako pięciokrotnie większy od rozmiaru struktury,
zgodnie z zaleceniem treści projektu (,,kilkukrotnie większy''). Rozmiary
kolejek dobrane do badań to: $1\,000$, $2\,000$, $5\,000$, $10\,000$, $20\,000$,
$50\,000$, $100\,000$.

\subsection{Schemat pomiaru}

Dla każdego rozmiaru $n$ wykonano \textbf{5 niezależnych powtórzeń}
całego eksperymentu, a wyniki uśredniono. W każdym powtórzeniu:
\begin{itemize}
    \item operacja \texttt{insert} -- mierzono łączny czas wstawienia $n$ elementów do pustej kolejki, a~wynik podzielono przez $n$ (czas amortyzowany pojedynczego wstawienia),
    \item operacja \texttt{peek}, \texttt{modifyKey}, \texttt{extractMax} -- na pełnej kolejce o~rozmiarze $n$ wykonywano serię $K = \min(1000, n)$ wywołań i mierzono ich łączny czas, a~następnie dzielono przez $K$,
    \item operacja \texttt{rozmiar} -- ze względu na bardzo małą złożoność wykonywano $100\,000$ wywołań i wynik dzielono przez tę liczbę.
\end{itemize}

Zgodnie z założeniami, dla operacji \texttt{peek} i \texttt{rozmiar}, których
wynik nie jest wykorzystywany, dodano akumulator z modyfikatorem \texttt{volatile}
aby zapobiec optymalizacji usuwającej wywołanie przez kompilator.

\section{Wyniki pomiarów}

W tabelach poniżej zebrano średnie czasy wykonania pojedynczej operacji
w nanosekundach dla obu implementacji. Operacje \texttt{peek} oraz
\texttt{rozmiar} mają złożoność $O(1)$ w obu strukturach i ich czasy są na
granicy rozdzielczości zegara, dlatego wartości należy traktować jako wskazanie
rzędu wielkości.

\subsection{Tabele wyników}

\begin{table}[H]
\centering
\caption{Kopiec binarny -- średnie czasy operacji [ns].}
\pgfplotstabletypeset[
    col sep=comma,
    columns/rozmiar/.style={column name={$n$}, column type=r, fixed, set thousands separator={\,}},
    columns/insert/.style={column name={insert}, column type=r, sci, sci zerofill, precision=3},
    columns/extract/.style={column name={extract}, column type=r, sci, sci zerofill, precision=3},
    columns/peek/.style={column name={peek}, column type=r, fixed, precision=2},
    columns/modify/.style={column name={modify}, column type=r, sci, sci zerofill, precision=3},
    columns/rozmiarOp/.style={column name={rozmiar}, column type=r, fixed, precision=2},
    every head row/.style={before row=\toprule, after row=\midrule},
    every last row/.style={after row=\bottomrule}
]{pomiary_kopiec.csv}
\end{table}

\begin{table}[H]
\centering
\caption{Lista wiązana -- średnie czasy operacji [ns].}
\pgfplotstabletypeset[
    col sep=comma,
    columns/rozmiar/.style={column name={$n$}, column type=r, fixed, set thousands separator={\,}},
    columns/insert/.style={column name={insert}, column type=r, sci, sci zerofill, precision=3},
    columns/extract/.style={column name={extract}, column type=r, sci, sci zerofill, precision=3},
    columns/peek/.style={column name={peek}, column type=r, fixed, precision=2},
    columns/modify/.style={column name={modify}, column type=r, sci, sci zerofill, precision=3},
    columns/rozmiarOp/.style={column name={rozmiar}, column type=r, fixed, precision=2},
    every head row/.style={before row=\toprule, after row=\midrule},
    every last row/.style={after row=\bottomrule}
]{pomiary_lista.csv}
\end{table}

\subsection{Wykresy porównawcze}

Wykresy zostały wykonane w skali logarytmicznej na obu osiach, co pozwala
łatwo odczytać charakter zależności (linia prosta na wykresie log-log oznacza
zależność potęgową).

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{loglogaxis}[
    xlabel={Rozmiar kolejki $n$},
    ylabel={Średni czas insert [ns]},
    legend pos=north west,
    grid=both,
    width=0.8\textwidth,
    height=0.55\textwidth
]
\addplot table [col sep=comma, x=rozmiar, y=insert] {pomiary_kopiec.csv};
\addplot table [col sep=comma, x=rozmiar, y=insert] {pomiary_lista.csv};
\legend{Kopiec binarny, Lista wiązana}
\end{loglogaxis}
\end{tikzpicture}
\caption{Czas operacji \texttt{insert(e, p)} w funkcji rozmiaru kolejki.}
\end{figure}

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{loglogaxis}[
    xlabel={Rozmiar kolejki $n$},
    ylabel={Średni czas extractMax [ns]},
    legend pos=north west,
    grid=both,
    width=0.8\textwidth,
    height=0.55\textwidth
]
\addplot table [col sep=comma, x=rozmiar, y=extract] {pomiary_kopiec.csv};
\addplot table [col sep=comma, x=rozmiar, y=extract] {pomiary_lista.csv};
\legend{Kopiec binarny, Lista wiązana}
\end{loglogaxis}
\end{tikzpicture}
\caption{Czas operacji \texttt{extractMax()} w funkcji rozmiaru kolejki.}
\end{figure}

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{loglogaxis}[
    xlabel={Rozmiar kolejki $n$},
    ylabel={Średni czas modifyKey [ns]},
    legend pos=north west,
    grid=both,
    width=0.8\textwidth,
    height=0.55\textwidth
]
\addplot table [col sep=comma, x=rozmiar, y=modify] {pomiary_kopiec.csv};
\addplot table [col sep=comma, x=rozmiar, y=modify] {pomiary_lista.csv};
\legend{Kopiec binarny, Lista wiązana}
\end{loglogaxis}
\end{tikzpicture}
\caption{Czas operacji \texttt{modifyKey(e, p)} w funkcji rozmiaru kolejki.}
\end{figure}

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{loglogaxis}[
    xlabel={Rozmiar kolejki $n$},
    ylabel={Średni czas peek [ns]},
    legend pos=north west,
    grid=both,
    width=0.8\textwidth,
    height=0.55\textwidth
]
\addplot table [col sep=comma, x=rozmiar, y=peek] {pomiary_kopiec.csv};
\addplot table [col sep=comma, x=rozmiar, y=peek] {pomiary_lista.csv};
\legend{Kopiec binarny, Lista wiązana}
\end{loglogaxis}
\end{tikzpicture}
\caption{Czas operacji \texttt{peek()} w funkcji rozmiaru kolejki.}
\end{figure}

\subsection{Analiza wyników}

\subsubsection{Operacja insert}

Wyniki potwierdzają oczekiwane złożoności obliczeniowe. Kopiec wykazuje
charakterystyczny wolny wzrost czasu wraz z rosnącym $n$ -- przy 100--krotnym
zwiększeniu rozmiaru (z $1\,000$ do $100\,000$) czas insertu rośnie tylko
około 3-krotnie, co odpowiada $\log_2(100\,000)/\log_2(1\,000) \approx 1{,}66$
po uwzględnieniu narzutów stałych i efektów cache. Lista wiązana zaś rośnie
zgodnie z oczekiwanym $O(n)$: czas wstawienia rośnie liniowo z rozmiarem
struktury, co dla $n = 100\,000$ daje czas pojedynczego wstawienia rzędu
milisekund -- około cztery rzędy wielkości większy niż dla kopca.

\subsubsection{Operacja extractMax}

Tu sytuacja się odwraca. Lista wykonuje \texttt{extractMax} w czasie $O(1)$
(zdjęcie głowy), niezależnie od rozmiaru, co potwierdzają stabilne czasy
rzędu kilkudziesięciu nanosekund. Kopiec wymaga heapify-down i ma złożoność
$O(\log n)$, więc czas rośnie -- dla $n = 100\,000$ jest to około
\SI{600}{\nano\second}.

\subsubsection{Operacja peek}

Obie struktury obsługują \texttt{peek} w czasie stałym -- dla kopca to korzeń
tablicy, dla listy głowa. Pomiary są na granicy rozdzielczości zegara
i~oscylują wokół 1--3 ns niezależnie od rozmiaru, co potwierdza złożoność
$O(1)$.

\subsubsection{Operacja modifyKey}

Operacja ta w obu implementacjach wymaga liniowego wyszukania elementu po
wartości, więc ma złożoność $O(n)$. Dane potwierdzają to: dla obu struktur
czas rośnie liniowo z rozmiarem. Lista jest jednak istotnie wolniejsza,
ponieważ poza wyszukaniem konieczne jest fizyczne wypięcie i ponowne
wstawienie węzła w odpowiednie miejsce, podczas gdy w kopcu wystarczy
naprawa struktury w $O(\log n)$.

Warto zauważyć efekt związany z gęstością wartości. Wartości elementów
losowane są z zakresu $[-1000, 1000]$ (czyli $2001$ różnych wartości),
podczas gdy rozmiar kolejki sięga $100\,000$. Oznacza to, że dla większych
$n$ szansa znalezienia elementu o szukanej wartości jest wysoka i znajdujemy
go już blisko początku tablicy/listy. Dla małych $n$ z kolei wiele
\texttt{modifyKey} kończy się przejściem całej struktury bez sukcesu. Z tego
powodu krzywa modifyKey dla kopca nie zawsze rośnie monotonicznie -- jest to
artefakt pomiaru, a nie błąd implementacji.

\subsubsection{Operacja rozmiar}

Obie implementacje przechowują licznik elementów aktualizowany przy każdym
\texttt{insert} i \texttt{extractMax}, więc \texttt{rozmiar} działa w czasie
$O(1)$. Pomiary potwierdzają to -- czasy rzędu pojedynczych nanosekund są
w~praktyce nierozróżnialne dla obu struktur.

\section{Wnioski}

\begin{enumerate}
    \item Wybór struktury danych do implementacji kolejki priorytetowej
          powinien zależeć od profilu obciążenia.
          \begin{itemize}
              \item Jeżeli głównym wzorcem użycia jest \texttt{insert} oraz
                    \texttt{extractMax} (typowy przypadek dla algorytmów
                    Dijkstry, Prima czy systemów obsługi zdarzeń) -- kopiec
                    binarny jest zdecydowanie lepszym wyborem dzięki
                    zrównoważeniu obu operacji w $O(\log n)$.
              \item Jeżeli operacje \texttt{insert} są bardzo rzadkie,
                    a~częste są \texttt{extractMax} lub \texttt{peek}, lista
                    wiązana posortowana ma stałe $O(1)$ na te operacje.
                    W praktyce takie scenariusze są rzadkie, bo i tak należy
                    najpierw wstawić elementy, co dla listy jest $O(n)$.
          \end{itemize}
    \item Pomiary potwierdziły teoretyczne złożoności obliczeniowe wszystkich
          rozważanych operacji. Kopiec binarny jest implementacją zrównoważoną,
          natomiast lista wiązana jest skrajnie zoptymalizowana pod
          \texttt{extractMax} i \texttt{peek} kosztem skrajnie powolnego
          \texttt{insert}.
    \item Strategia FIFO przy równych priorytetach została poprawnie
          zrealizowana w obu implementacjach (w kopcu poprzez tie-break
          po numerze wstawienia w komparatorze, a~w liście poprzez wstawienie
          nowego elementu za istniejące o tym samym priorytecie).
    \item Operacja \texttt{modifyKey} w obu strukturach jest w pesymistycznym
          przypadku $O(n)$ z powodu liniowego wyszukania elementu po
          wartości. Wprowadzenie indeksu (np. mapy wartość $\to$ pozycja)
          mogłoby przyspieszyć tę operację, ale wykraczałoby to poza zakres
          niniejszego projektu.
\end{enumerate}

\end{document}
